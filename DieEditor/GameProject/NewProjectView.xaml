<UserControl x:Class="DieEditor.GameProject.NewProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DieEditor.GameProject"
             mc:Ignorable="d" Width="800"
             d:DataContext="{d:DesignInstance Type=local:NewProject, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.DataContext>
		<local:NewProject/>
	</UserControl.DataContext>

	<Grid Background="Black">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="30"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="200"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<ListBox Grid.Row="0" Grid.Column="0"
                 Background="DarkGray" 
                 Foreground="White" 
                 FontSize="16"
                 x:Name="templateListBox"
				 SelectedIndex= "0"
                 ItemsSource="{Binding ProjectTemplates}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal" Margin="5">
						<Image Source="{Binding IconFilePath}" Width="32" Height="32" Margin="0,0,10,0"/>
						<TextBlock Text="{Binding ProjectType}" VerticalAlignment="Center"/>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<Grid Grid.Row="0" Grid.Column="1" Margin="5, 0">
			<Grid.RowDefinitions>
				<RowDefinition Height="*"/>
				<RowDefinition Height="100"/>
			</Grid.RowDefinitions>

			<Border Grid.Row="0" 
                    Margin="5,0,0,0" 
                    BorderBrush="Gray" 
                    BorderThickness="1">
				<Image Source="{Binding SelectedItem.ScreenshotFilePath, ElementName=templateListBox}" 
                       Stretch="Uniform"/>
			</Border>

			<Grid Grid.Row="1">
				<Grid.RowDefinitions>
					<RowDefinition Height="*"/>
					<RowDefinition Height="*"/>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>
				<Grid Grid.Row="0">
					<StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
						<TextBlock Text="Name:" Foreground="Azure" />
						<TextBox Text="{Binding ProjectName}" Height="20" FontSize="12"/>
					</StackPanel>
				</Grid>
				<Grid Grid.Row="1">
					<StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
						<TextBlock Text="Path:" Foreground="Azure"/>

						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="Auto"/>
							</Grid.ColumnDefinitions>

							<TextBox Text="{Binding ProjectPath}" Height="20" FontSize="12" VerticalAlignment="Center"/>
							<Button Grid.Column="1" Content="Browse" Width="100" Height="20" Margin="5,0,0,0"/>
						</Grid>
					</StackPanel>
				</Grid>
				<Grid Grid.Row="2" Margin="0">
					<StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
						<Button x:Name="CreateProjectButton"
								IsDefault="True" 
								IsEnabled="{Binding IsValid}"
								Click="CreateButton_Click" 
								Content="Create" 
								Width="100" 
								Height="20" 
								Margin="5,0"/>

						<Button x:Name="NewProjectButton"
								IsCancel="True" 
								Click="CancelButton_Click" 
								Content="Cancel" 
								Width="100" 
								Height="20" 
								Margin="0"/>
					</StackPanel>
				</Grid>

			</Grid>

		</Grid>
		<Grid Grid.Row="2" Grid.Column="1">
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
				<TextBlock Text="{Binding ErrorMsg}" Foreground="Red" />
			</StackPanel>
		</Grid>
	</Grid>
</UserControl>
